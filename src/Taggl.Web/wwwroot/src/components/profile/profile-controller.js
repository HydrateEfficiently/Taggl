import { Injectable } from './../../utility/injectable';

class ProfileController extends Injectable {
    static get $inject() {
        return [];
    }

    constructor(...deps) {
        super(...deps);

    }
}